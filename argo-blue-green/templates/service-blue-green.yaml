{{- define "argo-blue-green.serviceActive" -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "argo-blue-green.fullname" . }}-active
  namespace: {{ .Values.global.namespace | quote }}
  labels:
    {{ include "argo-blue-green.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{ include "argo-blue-green.selectorLabels" . | nindent 4 }}
  ports:
    {{- range .Values.service.specs }}
    - name: {{ .name }}
      port: {{ .port }}
      protocol: {{ .protocol | default "TCP" }}
      targetPort: {{ .targetPort | default .port }}
    {{- end }}
{{- end }}

---
{{- define "argo-blue-green.servicePreview" -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "argo-blue-green.fullname" . }}-preview
  namespace: {{ .Values.global.namespace | quote }}
  labels:
    {{ include "argo-blue-green.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{ include "argo-blue-green.selectorLabels" . | nindent 4 }}
  ports:
    {{- range .Values.service.specs }}
    - name: {{ .name }}
      port: {{ .port }}
      protocol: {{ .protocol | default "TCP" }}
      targetPort: {{ .targetPort | default .port }}
    {{- end }}
{{- end }}