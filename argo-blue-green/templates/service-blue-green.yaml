{{- define "bluegreen.serviceActive" -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "bluegreen.fullname" . }}-active
  namespace: {{ .Values.global.namespace | quote }}
  labels:
    {{ include "bluegreen.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{ include "bluegreen.selectorLabels" . | nindent 4 }}
  ports:
    {{- range .Values.service.specs }}
    - name: {{ .name }}
      port: {{ .port }}
      protocol: {{ .protocol | default "TCP" }}
      targetPort: {{ .targetPort | default .port }}
    {{- end }}
{{- end }}

---
{{- define "bluegreen.servicePreview" -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "bluegreen.fullname" . }}-preview
  namespace: {{ .Values.global.namespace | quote }}
  labels:
    {{ include "bluegreen.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{ include "bluegreen.selectorLabels" . | nindent 4 }}
  ports:
    {{- range .Values.service.specs }}
    - name: {{ .name }}
      port: {{ .port }}
      protocol: {{ .protocol | default "TCP" }}
      targetPort: {{ .targetPort | default .port }}
    {{- end }}
{{- end }}